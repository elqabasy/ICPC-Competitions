#include <iostream>
using namespace std;

/* Constraints
    - 0 <= A, B <= 1000
*/

int main() {
    int A, B;
    cin >> A >> B;

    if(0 <= A, B <= 1000){

        float result = float(A * B)  / 100;
        cout <<  result;
    }

    return 0;
}